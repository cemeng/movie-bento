<%= form_for(@movie_category) do |f| %>
  <% if @movie_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie_category.errors.count, "error") %> prohibited this movie_category from being saved:</h2>

      <ul>
      <% @movie_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %><br />
    <%= f.text_field :movie_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
